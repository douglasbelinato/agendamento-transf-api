# ==> Configuracoes H2 ---------------------------------------------------------------------
# URL padrao do console administrativa do H2: http://localhost:8080/h2-console/
spring.h2.console.enabled=true

spring.datasource.url=jdbc:h2:mem:mydb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=validate

# ==> Configuracoes Spring -----------------------------------------------------------------
server.port=8080

# ==> Configuracoes oAuth2 -----------------------------------------------------------------
api.oauth2.clientId=clientCvc
api.oauth2.clientSecret=s3cr3t
api.oauth2.host=http://localhost:
api.oauth2.tokenUri=/oauth/token
api.oauth2.accessToken.validity.seconds=3600